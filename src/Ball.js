/*export default class Ball {
constructor(x,y,speed){
  this.x = 150
  this.y = 50
  this.vx = 1
  this.vy = 1
  this.speed = 5
  }
}

render(context){
context.fillStyle = this.color
    context.fillRect(
      this.x,
      this.y,
      this.width,
      this.height

}